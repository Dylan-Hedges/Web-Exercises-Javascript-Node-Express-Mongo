<% include partials/header %>

<h1>The Posts Page</h1>

<h2>forEach loop (more common)</h2>

<!--We remove the [i] and only use "post" as we are using a forEach-->
<% posts.forEach(function(post){ %>
    <li>The title is: <%= post.title %>  - The author is: <strong><%= post.author %></strong></li>
<% }); %>

<h2>for loop</h2>

<!--We need to include the index variable [i] when using a for loop-->
<% for (var i=0; i < posts.length; i++){ %>
    <li>The title is: <%= posts[i].title %>  - The author is: <strong><%= posts[i].author %></strong></li>
<% } %>

<% include partials/footer %>
